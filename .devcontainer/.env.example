# Template — copy to .env and adjust paths for your machine.
# Compose auto-loads .env from the same directory (docker-compose.yml).
#
# RADAR_TENSORRT_STAGE: host dir staged with TensorRT/CUDA headers+libs,
# produced by: RADAR_TENSORRT_STAGE=<dir> .script/prepare-radar-camera-tensorrt-runtime
# Mounted into the container at /opt/radar_camera_trt (read-only).
RADAR_TENSORRT_STAGE=/home/yukikaze/radar_trt_stage
